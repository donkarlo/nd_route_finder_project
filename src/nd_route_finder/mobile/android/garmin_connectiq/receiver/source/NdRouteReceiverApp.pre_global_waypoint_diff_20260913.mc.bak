import Toybox.Application;
import Toybox.Communications;
import Toybox.Position;
import Toybox.PersistedContent;
import Toybox.System;
import Toybox.Timer;
import Toybox.WatchUi;

var gReceiverStatus = "Waiting for phone…";

class NdRouteReceiverApp extends Application.AppBase {
    var _openTimer = null;
    var _openAttempts = 0;

    function initialize() {
        AppBase.initialize();
        _openTimer = new Timer.Timer();
    }

    function onStart(state) {
        gReceiverStatus = "Waiting for route target…";
        Communications.registerForPhoneAppMessages(method(:onPhoneMessage));
    }

    function onStop(state) {
        if (_openTimer != null) {
            _openTimer.stop();
        }
    }

    function getInitialView() {
        return [ new NdRouteReceiverView() ];
    }

    function onPhoneMessage(message as Communications.PhoneAppMessage) as Void {
        try {
            if (message == null || message.data == null) {
                gReceiverStatus = "Empty phone message";
                WatchUi.requestUpdate();
                return;
            }

            var data = message.data;
            var latitude = data["lat"];
            var longitude = data["lon"];
            var pointName = data["name"];
            if (pointName == null) {
                pointName = "ND point";
            }

            var location = new Position.Location({
                :latitude => latitude,
                :longitude => longitude,
                :format => :degrees
            });

            // Keep the app-owned waypoint collection deterministic: remove
            // waypoints previously created by this receiver before saving the
            // new navigation target. User/system waypoints are untouched.
            var oldItems = PersistedContent.getAppWaypoints();
            var oldItem = oldItems.next();
            while (oldItem != null) {
                oldItem.remove();
                oldItem = oldItems.next();
            }

            PersistedContent.saveWaypoint(location, { :name => pointName });

            _openAttempts = 0;
            gReceiverStatus = "Opening activity chooser…";
            WatchUi.requestUpdate();

            // On Fenix 8 the newly saved app waypoint may take a while to
            // become visible. Poll only the app-owned collection. Since it was
            // cleared above, the first waypoint that appears is the new target.
            _openTimer.stop();
            _openTimer.start(method(:tryOpenWaypoint), 250, true);
        } catch (error) {
            gReceiverStatus = "Target error";
            System.println(error);
            WatchUi.requestUpdate();
        }
    }

    function tryOpenWaypoint() {
        try {
            _openAttempts += 1;

            var items = PersistedContent.getAppWaypoints();
            var item = items.next();

            if (item != null) {
                _openTimer.stop();
                gReceiverStatus = "Choose activity…";
                WatchUi.requestUpdate();
                System.exitTo(item.toIntent());
                return;
            }

            if (_openAttempts >= 80) {
                _openTimer.stop();
                gReceiverStatus = "Waypoint saved; open Locations";
                WatchUi.requestUpdate();
            }
        } catch (error) {
            _openTimer.stop();
            gReceiverStatus = "Open navigation error";
            System.println(error);
            WatchUi.requestUpdate();
        }
    }
}
